import { expect } from "chai";

describe("Math function", function () {
    it("Addiert zwei Zahlen korrekt", function () {
        const result = 5;
        expect(result).to.equal(5); // Verwendet den toBe-Matcher
    });
});
